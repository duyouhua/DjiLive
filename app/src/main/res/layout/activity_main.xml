<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/activity_main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context="danielckv.com.djilive.MainActivity">

    <TextView
        android:text="Live FB Video from Air"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:id="@+id/textView"
        android:textSize="30sp"
        app:layout_constraintTop_toTopOf="@+id/activity_main"
        android:layout_marginTop="16dp"
        android:textAlignment="center"
        app:layout_constraintBottom_toTopOf="@+id/textView2"
        android:layout_marginBottom="8dp" />

    <TextView
        android:layout_width="352dp"
        android:layout_height="64dp"
        android:text="Before we are starting we need to ask your permission for Facebook live feed."
        android:textAppearance="@android:style/TextAppearance.DeviceDefault.Medium"
        android:typeface="normal"
        android:lineSpacingExtra="10sp"
        android:id="@+id/textView2"
        app:layout_constraintLeft_toLeftOf="@+id/activity_main"
        app:layout_constraintTop_toTopOf="@+id/activity_main"
        app:layout_constraintRight_toRightOf="@+id/activity_main"
        app:layout_constraintBottom_toBottomOf="@+id/activity_main"
        app:layout_constraintHorizontal_bias="0.56"
        app:layout_constraintVertical_bias="0.29" />

    <com.facebook.login.widget.LoginButton
        android:id="@+id/login_button"
        android:layout_width="208dp"
        android:layout_height="wrap_content"
        android:layout_gravity="center_horizontal"
        tools:layout_editor_absoluteX="79dp"
        tools:layout_editor_absoluteY="271dp"
        android:
        android:background="@drawable/com_facebook_button_like_background" />

</android.support.constraint.ConstraintLayout>
